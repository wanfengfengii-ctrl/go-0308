# 市政饮用水管段冲洗消毒与通水放行闭环

## 项目目标

面向新建市政饮用水管段，管理从管段锁定、阀门隔离、定向冲洗、消毒剂注入、有效接触到分点采样和通水放行的完整流程。Go HTTP 后端使用关系数据库持久化管段拓扑、阶段事件、测量值、样本链、复验轮次和唯一终局，服务重启后恢复逻辑时钟、有效占用、待重试检测调用和当前轮次。真实前端连接 API，展示拓扑、采样点、阶段证据、异常传播和裁定结果。生产代码要求 20—26 个 Go 文件、约 2500 有效行并控制在 2000—3000 行，分布于至少 5 个业务包；测试、前端、注释与生成内容不计入该额度。支持 amd64、arm64 Docker 构建。

## 端到端业务流程

1. 创建作业并锁定管段拓扑：登记整数毫米管径、整数米长度、节点、阀门、排放口、注入点、分支盲端、采样点、流向、估算容积、目标流速、消毒剂浓度与接触时长；锁定后生成不可变摘要和首轮编号。
2. 执行隔离验证：按阀门清单提交开闭位置、复核人员和压力稳定读数；系统验证目标管段与既有供水边界断开、排放路径可达，满足后才允许冲洗。
3. 执行定向冲洗和消毒：在有效资源占用下记录流量计、浊度计、余氯仪读数；以逻辑时间累计连续达标窗口，完成精确投加量、管内容积置换倍数和有效接触时长计算，失败写入可重试调用但不推进阶段。
4. 完成接触后按锁定顺序采样：每个采样点生成防重标签，登记采集、封存、交接和检测回执；回执必须匹配样本摘要、轮次、检测项目及校准状态。
5. 发现浊度、余氯、微生物、隔离或样本链异常时，根据共同管段、上游传播、分支盲端和同次检测运行生成稳定排序的复验集合；处置后创建递增轮次，旧证据保留且不得覆盖当前结果。
6. 两名不同合格人员完成独立复核后申请终局裁定；仲裁器检查拓扑隔离、阶段前缀、连续达标窗口、接触条件、全部采样点、检测结论及未关闭异常，并保证并发请求只持久化一个终局。

## 核心组件与职责

1. 规则目录与作业锁定器：维护版本化阈值、仪器校准规则、人员资格和管段模板，锁定不可变拓扑、采样方案、阶段依赖及规则摘要。
2. 管段拓扑与流程聚合：以只追加事件维护节点、阀门、流向、当前轮次、阶段前缀和异常状态，并在启动时确定性重建当前视图。
3. 水力消毒计算与资源协调器：使用整数定点数计算容积、置换倍数、投加量、连续达标窗口和接触量，管理排放口、注入泵、检测仪器及采样位的限时互斥占用。
4. 样本链与检测适配器：生成唯一标签，验证采集、封存、交接和回执链，保存脚本化检测调用、校准状态、重试序号及迟到结果。
5. 异常复验与终局仲裁器：确定性计算影响集合、创建新轮次、隔离旧轮次回执，验证双人复核并通过数据库唯一约束写入终局凭据。
6. HTTP 服务与作业前端：提供作业、占用、证据、样本、复验和裁定 API；前端通过真实接口呈现管段拓扑、阀门状态、读数时间线、样本链和终局结果。

## 领域规则与不变量

1. 长度、管径、流量、压力、浓度、时间和菌落计数均使用整数或带明确比例尺的定点数；乘除前检查负值、除零及有符号溢出，统一采用向半远离零舍入。
2. 锁定拓扑必须连通且流向无环；目标管段的每个外部连接必须由已验证关闭的边界阀隔离，每个盲端必须关联可执行的冲洗或采样措施。
3. 阶段严格遵循拓扑锁定、隔离验证、定向冲洗、消毒注入、有效接触、排放复冲、分点采样、检测复核、终局裁定；错误轮次、缺少前置证据或失效资源占用不能形成有效事件。
4. 冲洗达标必须在连续逻辑时间窗口内同时满足最低流速和最高浊度；接触合格必须满足最低初始浓度、最低末端浓度和浓度时间积，窗口中断后从零重新累计。
5. 每个锁定采样点在当前轮次只能有一个有效样本；标签不可复用，采集人、封存人、交接时间和检测回执必须完整，回执摘要或检测项目不匹配时不得形成结论。
6. 复验只能创建严格递增轮次。旧轮次事件不可修改；旧轮次或较早逻辑时间的迟到回执可保存为历史记录，但不能改变当前阶段、异常状态或终局。
7. 资源键在逻辑时间内互斥；过期持有者的提交必须拒绝。投加证据、材料消耗、资源占用和幂等回执必须在单个数据库事务中提交或整体回滚。
8. 所有写入使用操作号与规范化请求摘要实现幂等；相同内容返回原结果，不同内容返回稳定冲突码。原因按管段、节点、采样点、检测项目和轮次排序，终局一经写入不可逆。

## 数据模型与持久化

1. RuleVersion、Qualification、InstrumentRule：阈值、定点比例尺、校准期限和人员资格；LockedJob：拓扑摘要、规则摘要、逻辑时钟、阶段与当前轮次。
2. PipeNode、PipeSection、ValveBoundary、FlushOutlet、InjectionPoint、SamplingPoint：规范化标识、整数尺寸、连接关系、流向、盲端标记和目标参数。
3. WorkflowEvent、MeasurementSeries、ChemicalDose、ResourceLease：只追加阶段事件、定点读数、投加信息、连续窗口及资源有效期。
4. Sample、CustodyEvent、LabAttempt、LabResult：标签、采集点、轮次、封存交接链、请求摘要、重试序号、校准状态和检测结果。
5. Incident、RetestSet、TreatmentRound：异常种子、传播依据、稳定成员、新旧轮次关联与关闭状态；ReviewDecision、TerminalVerdict 保存复核摘要、凭据号、原因和提交序号。
6. OperationReceipt、OutboxEvent、SchemaMigration：幂等响应、事务提交号、待投递事件和模式版本，用于重启恢复。

## 公开接口

1. POST /api/jobs 与 POST /api/jobs/{id}/lock 创建并锁定作业；GET /api/jobs/{id}、/topology、/timeline 返回规范排序的当前状态。
2. POST /api/jobs/{id}/leases/acquire 与 /release 管理资源；POST /api/jobs/{id}/stages/{stage}/evidence 提交阀位、压力、流量、浊度、余氯、投加和接触证据。
3. POST /api/jobs/{id}/samples 创建样本；POST /api/samples/{id}/custody 追加交接；POST /api/lab-attempts/{id}/retry 与 /result 处理确定性检测调用。
4. POST /api/jobs/{id}/incidents 创建异常；GET /api/jobs/{id}/retests 查询影响依据；POST /api/jobs/{id}/retests/{retestID}/start 创建处置轮次。
5. POST /api/jobs/{id}/reviews 提交独立复核；POST /api/jobs/{id}/terminal 请求终局裁定并返回唯一凭据或稳定排序原因。
6. GET / 提供可构建前端，包含管段拓扑、阀门隔离矩阵、读数时间线、连续窗口、样本链、复验集合和终局操作区；固定依赖与锁文件，构建产物由 Go 服务托管。

## 失败边界

1. 作业锁定、阶段事件、投加消耗、资源占用和幂等回执受事务保护；任一约束失败均不得留下部分状态。
2. 拓扑断裂、流向成环、外部连接未隔离、阀位矛盾、盲端无措施、阶段越序、逻辑时间倒退及轮次冲突在持久化前拒绝。
3. 定点计算遇到负值、除零、比例尺不一致、溢出或区间外结果时返回明确错误，不使用浮点降级或截断结果。
4. 仪器或检测调用出现拒绝、断连、超时、校准过期、格式错误时仅记录待重试状态，不产生读数、结论或阶段推进。
5. 异常传播与新轮次创建采用数据库锁和轮次比较交换；竞争失败返回稳定冲突码，旧轮次迟到结果不进入当前视图。
6. 终局以作业唯一键和提交序号形成单写屏障；并发裁定只有首个合法请求成功，其余读取已持久化结果。

## 验收标准

1. 锁定后拓扑、边界阀、流向、盲端措施、采样点、目标参数和规则摘要保持不可变；陈旧规则摘要无法锁定。
2. 确定性识别拓扑断裂、环路、未隔离外连和无措施盲端，并按规范键返回稳定原因。
3. 以整数定点规则精确计算管内容积、置换倍数、投加量、连续达标窗口和浓度时间积，覆盖阈值等号、窗口中断、舍入、除零和溢出。
4. 资源竞争、投加消耗与阶段写入原子提交；幂等重放返回同一结果，内容冲突或竞争失败不留下部分事件。
5. 每个当前轮次采样点仅接受一个有效样本，完整保存封存交接链；错误摘要、项目、校准状态及旧轮次回执不得生成当前结论。
6. 异常按共同管段、上游传播、盲端和检测运行生成唯一稳定复验集合；新轮次保留全部历史且重启后恢复一致视图。
7. 仅在全部阶段、连续窗口、接触指标、采样点、检测结论和双人复核满足规则时生成唯一通水凭据；并发终局请求只能产生一个结果。
8. 项目包含至少 20 个生产 Go 文件、至少 2000 有效生产行且目标约 2500 行，覆盖至少 4 个业务包；至少 4 个公开测试文件和 12 个公开测试，前端可构建并实际调用后端，双架构容器构建成功。

## 确定性测试场景

1. 至少设置 topology_lock_test.go、hydraulic_window_test.go、sample_retest_test.go、terminal_recovery_test.go 四个公开测试文件，共不少于 12 个独立测试，并使用固定逻辑时钟和临时数据库。
2. 用固定节点边集验证合法拓扑、断裂、流向环、未隔离外连、矛盾阀位和无措施盲端的精确结果及排序。
3. 用公开管径、长度、流量和浓度序列断言容积、置换倍数、投加量、窗口重置、浓度时间积、阈值等号、舍入与溢出分支。
4. 使用同步屏障争抢同一排放口、注入泵和仪器，断言仅一方成功，败方不留下投加消耗、占用或阶段事件。
5. 验证相同操作号同内容重放、不同内容冲突、阶段跳跃、逻辑时间倒退、资源过期和错误轮次。
6. 脚本化仪器成功、拒绝、断连、超时、校准过期和格式错误，精确断言重试号、待重试状态及无伪造读数。
7. 验证样本标签防重、完整交接链、回执摘要匹配、检测项目匹配，以及服务重启后未完成检测调用恢复。
8. 从浊度超限、余氯不足、微生物异常、隔离失效和样本链破损出发，断言规范化影响集合和递增处置轮次。
9. 提交旧轮次迟到回执，断言历史可查询而当前阶段、异常及检测结论不变。
10. 构造一条完整合格流程和多条缺证流程，分别断言唯一凭据及稳定排序原因。
11. 并发提交不同终局请求并重复执行，断言数据库始终只有一条终局；重启后读取相同提交序号与凭据。
12. 构建前端后启动服务，通过 HTTP 完成锁定、证据提交、样本查询和裁定，验证页面数据来自持久化 API。

## 组件追踪关系

1. 规则目录与锁定器覆盖规则摘要、人员资格、不可变拓扑和陈旧版本测试。
2. 拓扑与流程聚合覆盖连通性、隔离边界、阶段前缀、只追加历史及重启重建测试。
3. 水力消毒计算与资源协调器覆盖定点计算、连续窗口、投加原子性、资源竞争和过期提交测试。
4. 样本链与检测适配器覆盖标签唯一性、封存交接、调用重试、校准校验和迟到回执测试。
5. 异常复验与终局仲裁器覆盖影响传播、轮次隔离、双人复核、唯一凭据和并发终局测试。
6. HTTP 服务与作业前端覆盖接口错误码、规范排序、持久化查询、真实交互页面和双架构容器构建。

## 独特性

项目以饮用水管段的有向拓扑、边界阀隔离、连续冲洗窗口、浓度时间积和分点样本链为统一证据模型，并将异常沿水流方向传播到确定复验集合，最终形成可恢复的唯一通水裁定。
